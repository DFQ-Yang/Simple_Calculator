## Simple Calculator

### functions
#### this is an android app that fulfilled simple functions of `+, -, *, %, e, %` operations
#### it allows result calculation(use the output of last calculation to continue to do calculation without copying)

### Improvement plan
#### Adjust layout: So that result/expression shown above won't pile all together
#### Edge cases processing: Some calculation may trigger Errors
#### Error processing: When working with invalid calculations, the app will not process, Need to directly show the `Error` as result
#### More functions: Like `power, quotaions, pi` etc
#### Historical calculation records: Record past calculation results, write them into disk, so that records will restore as app crashed or user exit
